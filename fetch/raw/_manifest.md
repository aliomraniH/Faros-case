# fetch/raw — manifest

One line per fetched page. Cowork/Desktop appends here after each fetch.

| url | tab | retrieved_at | file |
|---|---|---|---|
| _(none yet — run fetch/INSTRUCTIONS.md)_ | | | |

> `_example.json` in this folder is a hand-written reference shape (NOT a live fetch) so the curator
> agent can see the expected structure. Delete or ignore it once real fetches land.
